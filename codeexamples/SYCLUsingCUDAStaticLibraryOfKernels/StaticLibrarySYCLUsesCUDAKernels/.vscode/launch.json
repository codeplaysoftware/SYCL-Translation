{
    "configurations": [
        {
            "name": "C/C++: Intel icpx build launch cuda-gdb debug CUDA target DXTC",
            "type": "cuda-gdb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/${config:programName}_d",
            "stopAtEntry": true,
            "cwd": "${fileDirname}",
            "environment": [],
            "preLaunchTask": "DXTC Debug C/C++: Intel icpx build CUDA target"
        },
        {
            "name": "C/C++: Intel icpx build attach cuda-gdb debug CUDA target DXTC",
            "type": "cuda-gdb",
            "request": "attach",
            "processId": "${command:cuda.pickProcess}"
        }
    ]
}
